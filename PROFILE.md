# 주가 예측 리포트 자동화 시스템 — 일반 RAG 대비 특장점

## 1. 자기 검증 + 점진적 자가 보정 (Self-Healing RAG)

일반 RAG는 LLM 응답을 그대로 반환하지만, 이 시스템은 **LLM 응답을 스스로 검증**한다.
신뢰도(`confidence_score < 7`) 또는 근거 수(`evidence_count < 3`) 미달 시 자동 재시도하며, 시도마다 전략을 **점진적으로 강화**한다.

| 시도 | 자동 조정 |
|------|-----------|
| 1차 재시도 | RAG 검색 범위 확장 (top_k 5 → 10) |
| 2차 재시도 | Few-shot 예시 주입 |
| 3차 실패 | 저신뢰 경고 포함 강제 생성 |

단순히 "재시도"가 아니라, **검색 파라미터와 프롬프트 구조를 단계적으로 바꾸는 점**이 핵심.

---

## 2. 외부 시장 신호 기반 동적 프롬프트 분기 (Context-Aware Prompting)

일반 RAG는 쿼리가 동일하면 항상 같은 프롬프트를 사용하지만, 이 시스템은 **실시간 시장 데이터(SOX 지수 ±3%)를 감지하여 시나리오를 분류**하고, 시나리오별로 시스템 프롬프트를 동적으로 변경한다.

→ 동일한 종목 예측이라도, 미국 반도체 시장 급변 시 완전히 다른 분석 관점으로 전환됨.

---

## 3. 정형 데이터 + 비정형 RAG 하이브리드 컨텍스트

일반 RAG는 벡터DB 검색 결과만 컨텍스트로 사용하지만, 이 시스템은 **3계층 컨텍스트**를 구성한다:

| 계층 | 데이터 유형 | 소스 |
|------|------------|------|
| 벡터DB RAG | 비정형 (뉴스, 공시, 리포트) | Chroma / Pinecone |
| 국내 참고 종목 | 정형 (등락률, 거래량) | KRX 실시간 수집 |
| 미국 반도체 동향 | 정형 (SOX, 5종목 등락률) | yfinance 수집 |

→ RAG 청크만으로는 파악할 수 없는 **업종 전반의 수급 흐름**을 LLM에 함께 제공.

---

## 4. 전 계층 전략 패턴 — 설정 한 줄로 기술 스택 교체

청킹·임베딩·벡터DB·LLM 4개 계층 모두 **Strategy Pattern ABC**로 추상화되어 있어, `config/pipeline.json` 또는 CLI 플래그 하나로 교체 가능.

```
청킹:   recursive | sentence | token | fixed
임베딩: bge-m3 | upstage | openai
벡터DB: chroma | pinecone
LLM:    claude | openai | gemini
```

→ 모델 벤치마크, A/B 테스트, 비용 최적화를 **코드 변경 없이** 수행할 수 있는 구조.

---

## 5. 그래프 기반 오케스트레이션 (LangGraph StateGraph)

절차형 스크립트가 아닌 **LangGraph의 StateGraph + conditional edge**로 파이프라인을 구성.
상태(`PipelineState`)가 노드 간 명시적으로 전파되어, 분기·루프·재시도가 선언적 그래프로 표현된다.

→ 노드 추가·분기 조건 변경이 엣지 선언만으로 가능하여 **확장성**이 높음.

---

## 6. 수집부터 발송까지 완전 자동화 End-to-End 파이프라인

대부분의 RAG 프로젝트는 "검색 + 생성" 부분만 구현하지만, 이 시스템은 **8개 소스 병렬 수집(ThreadPoolExecutor) → 텍스트 추출 → 청킹 → 임베딩 → 벡터DB → RAG 예측 → 오케스트레이션 → Slack Block Kit 리포트 발송**까지 전 과정을 자동화하고, GitHub Actions cron으로 평일 매일 스케줄 실행된다.

각 단계는 **스냅샷 기반 멱등성**을 가져 중간에 실패해도 재실행 시 완료된 단계를 건너뛴다.

---

## 한 줄 요약 (이력서 기재용)

> 8개 소스 병렬 수집 → 전략 패턴 기반 RAG(청킹·임베딩·벡터DB·LLM 4계층 교체 가능) → LangGraph 그래프 오케스트레이션(외부충격 시나리오 자동 감지 + 신뢰도 자가 검증·점진적 재시도) → Slack 리포트 자동 발송까지의 End-to-End 주가 예측 파이프라인
